<div class="col-lg-2 text-right menu-h4">
  <h4><%= t '.cuisines'%>:</h4>
</div>
<div class="col-lg-10">
  <ul class="nav nav-pills nav-justified">
    <% $cuisines.first(7).each do |cuisine| %>
      <li role="presentation"><%= link_to cuisine.name, cuisine %></li>
    <% end %>
      <li role="presentation" class="dropdown">
      <a class="dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
        More <span class="caret"></span>
      </a>
      <ul class="dropdown-menu">
        <% $cuisines.drop(7).each do |cuisine| %>
          <li role="presentation"><%= link_to cuisine.name, cuisine %></li>
        <% end %>
      </ul>
    </li>
  </ul>
</div>

<div class="col-lg-2 text-right menu-h4">
<h4><%= t '.preferences'%>:</h4>
</div>
<div class="col-lg-10">
<ul class="nav nav-pills nav-justified">
  <% $preferences.first(7).each do |preference| %>
    <li role="presentation"><%= link_to preference.name, preference %></li>
  <% end %>
    <li role="presentation" class="dropdown">
    <a class="dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
      More <span class="caret"></span>
    </a>
    <ul class="dropdown-menu">
      <% $preferences.drop(7).each do |preference| %>
        <li role="presentation"><%= link_to preference.name, preference %></li>
      <% end %>
    </ul>
  </li>
</ul>
</div>

<div class="col-lg-2 text-right menu-h4">
<h4><%= t '.courses'%>:</h4>
</div>
<div class="col-lg-10">
<ul class="nav nav-pills nav-justified">
  <% $courses.first(7).each do |course| %>
    <li role="presentation"><%= link_to course.name, course %></li>
  <% end %>
    <li role="presentation" class="dropdown">
    <a class="dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
      More <span class="caret"></span>
    </a>
    <ul class="dropdown-menu">
      <% $courses.drop(7).each do |course| %>
        <li role="presentation"><%= link_to course.name, course %></li>
      <% end %>
    </ul>
  </li>
</ul>
</div>
